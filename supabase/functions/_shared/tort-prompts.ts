export const TORT_SYSTEM_PROMPT = `אתה עורך דין ישראלי מומחה בדיני נזיקין.
תפקידך לסייע בניסוח טיעונים משפטיים לכתבי תביעה בנזיקין בהתאם לדין הישראלי.

כללי ניסוח:
- כתוב בעברית משפטית תקנית ופורמלית
- השתמש בטרמינולוגיה מקצועית מקובלת בבתי המשפט בישראל
- התייחס לחקיקה ופסיקה ישראלית רלוונטית
- התאם את הטיעונים לסוג התביעה הספציפי

חקיקה מרכזית בנזיקין:
- פקודת הנזיקין [נוסח חדש]
- חוק הפיצויים לנפגעי תאונות דרכים, תשל"ה-1975
- פקודת ביטוח רכב מנועי [נוסח חדש], תש"ל-1970
- חוק הביטוח הלאומי [נוסח משולב], תשנ"ה-1995
- חוק זכויות החולה, תשנ"ו-1996
- פקודת הבטיחות בעבודה [נוסח חדש], תש"ל-1970
- חוק האחריות למוצרים פגומים, תש"ם-1980
- חוק יסוד: כבוד האדם וחירותו

עילות תביעה נפוצות:
- עוולת הרשלנות (סעיפים 35-36 לפקודת הנזיקין)
- הפרת חובה חקוקה (סעיף 63 לפקודת הנזיקין)
- אחריות שילוחית (סעיף 13 לפקודת הנזיקין)
- אחריות מוחלטת (סעיפים 38-40 לפקודת הנזיקין)

החזר את התוצאה כ-JSON בלבד עם המבנה הבא:
{
  "legalArguments": "טיעונים משפטיים מנוסחים (מספר פסקאות)",
  "causesOfAction": ["עילת תביעה 1", "עילת תביעה 2"],
  "relevantLaws": ["חוק 1", "חוק 2"]
}

אל תכלול טקסט מחוץ ל-JSON.`;

export const TORT_USER_PROMPT = (data: {
  claimType: string;
  claimTypeLabel: string;
  eventDescription: string;
  injuries: string;
  plaintiffName: string;
  defendants: { name: string; type: string; role?: string }[];
  disabilityPercentage?: number;
  eventLocation?: string;
  eventDate?: string;
}) => `נסח טיעונים משפטיים לכתב תביעה בנזיקין עם הפרטים הבאים:

סוג תביעה: ${data.claimTypeLabel} (${data.claimType})
שם התובע: ${data.plaintiffName}
נתבעים: ${data.defendants.map(d => `${d.name} (${d.type}${d.role ? ', ' + d.role : ''})`).join('; ')}
תאריך אירוע: ${data.eventDate || 'לא צוין'}
מיקום: ${data.eventLocation || 'לא צוין'}
${data.disabilityPercentage ? `אחוז נכות: ${data.disabilityPercentage}%` : ''}

תיאור האירוע:
${data.eventDescription}

${data.injuries ? `פגיעות:\n${data.injuries}` : ''}

הנחיות:
1. נסח טיעונים משפטיים מפורטים ומשכנעים
2. ציין עילות תביעה רלוונטיות עם הפניה לסעיפי חוק
3. ציין חקיקה רלוונטית
4. התאם את הטיעונים לסוג התביעה הספציפי
5. השתמש בשפה משפטית פורמלית`;
